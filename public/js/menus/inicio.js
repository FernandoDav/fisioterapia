shell("Buenos dias, Mariana", "inicio");
const revenue = db.invoices.reduce((sum, item) => sum + Number(item.amount), 0);
document.querySelector("#menuContent").innerHTML =
  `<div class="content-heading"><div><p class="eyebrow">RESUMEN GENERAL</p><h2>Resumen de la clinica</h2><p>Una vista clara de lo que sucede hoy.</p></div><span class="date-pill">24 Ago, 2026</span></div><div class="kpis"><div class="kpi"><span>Ingresos del mes</span><strong>${money(revenue)}</strong><i>↑ 12.8% vs. mes anterior</i></div><div class="kpi"><span>Pacientes activos</span><strong>${db.patients.length}</strong><i>↑ 8 nuevos este mes</i></div><div class="kpi"><span>Sesiones hoy</span><strong>${db.appointments.length}</strong><i>Agenda actualizada</i></div><div class="kpi"><span>Por cobrar</span><strong>${money(0)}</strong><i>Todo al dia</i></div></div><div class="dashboard-grid"><section class="panel"><div class="panel-title"><h3>Ingresos del periodo</h3><a href="estadisticas.html">Ver detalle →</a></div><div class="bar-chart">${[55, 70, 45, 82, 68, 94, 76].map((height, index) => `<i style="height:${height}%" class="${index === 6 ? "current" : ""}"></i>`).join("")}</div></section><section class="panel"><div class="panel-title"><h3>Agenda de hoy</h3><a href="agenda.html">Ver agenda →</a></div>${db.appointments.map((item) => `<p class="agenda-row"><b>${item.time}</b> ${item.patient}<small>${item.employee}</small></p>`).join("") || '<p class="empty">No hay citas.</p>'}</section></div>`;
