# Vistas

Este directorio contiene un HTML independiente por cada menu. Todos se pueden abrir directamente en el navegador y comparten los datos temporales de `localStorage` mediante `public/js/common.js`.
